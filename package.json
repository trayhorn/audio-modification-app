{
  "name": "audio-modification-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "concurrently -c 'blue,magenta,cyan' --names 'Server, Client' 'cd server && npm start' 'cd client && npm run dev'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
